using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public struct SingleBody
{
    // Start is called before the first frame update
    public Vector3 position;
    public Vector4 quat;
}
